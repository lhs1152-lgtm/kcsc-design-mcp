# 판올림 내역

날짜 형식은 `YYYY-MM-DD` 입니다.

## 0.3.0 — 2026-08-25

### 뺀 것
- **`design_sheet`(빈 단면검토 엑셀)를 없앴습니다.** 도구가 14개에서 13개가 됩니다.

  이 도구는 **빈 서식**만 만들었습니다 — 계산식도 값도 넣지 않았습니다(원문 수식이
  이미지라 알 수 없고, 모르는 식을 넣으면 그게 사고이기 때문입니다). 그런데 빈 서식은
  설계자가 쓰던 자기 양식이 이미 있고, 파일을 남의 PC 에 만들어 두는 것치고는
  얻는 것이 적었습니다. 검토 순서와 근거는 `design_flow` 가 그대로 냅니다.

- 그에 따라 **`openpyxl` 의존성도 뺐습니다.** 설치가 그만큼 가벼워집니다.

### 고친 것
- **조합력부재 트리 이름에서 단면 표기를 뺐습니다.** `조합력부재 (압축 + 휨) / H형강`
  → `조합력부재 (압축 + 휨)`.

  이 트리는 H형강·원형강관·각형강관을 **모두** 다루는데(파일 안에도 그렇게 적혀
  있습니다) 이름에는 H형강만 보여, 각형강관을 검토하던 설계자가 "내 단면은 트리가
  없구나" 하고 지나칠 자리였습니다. 트리 사이의 이음 23곳을 함께 고쳤습니다.

- 패키지가 알리던 판 번호가 `0.2.1` 에 멈춰 있던 것을 고쳤습니다. 설치본은 0.3.0 인데
  MCP 연결 시 서버가 자기를 0.2.1 이라고 말했습니다.

### 쓰던 분께
`design_sheet` 를 부르면 이제 "그런 도구가 없다"고 나옵니다. 대신 `design_flow` 로
검토 순서·입력값·근거 조항을 받아 쓰던 양식에 옮겨 쓰십시오. 내용은 같습니다.

**자기 결정트리를 만들어 쓰시는 분**은 `다음트리:` 에 적은
`"조합력부재 (압축 + 휨) / H형강"` 을 `"조합력부재 (압축 + 휨)"` 으로 고치십시오.
설계법까지 적으셨다면 단면 자리는 비워 둡니다 —
`"조합력부재 (압축 + 휨) / / 하중저항계수설계법 (LRFD)"`.
고치지 않으면 그 이음에서 트리를 못 찾습니다.

## 0.2.2 — 2026-08-24

### 더한 것
- **수식 그림을 텍스트로 옮겨 본문에 넣을 수 있습니다.** 국가건설기준의 수식·기호는 원문이
  GIF 이미지라(`alt` 도 MathML 도 없습니다) 본문이 `〔그림〕` 으로 도배됐습니다. 이제 사전을
  주면 그 자리에 식을 글자로 넣습니다 — `〔그림〕 = 〔그림〕·〔그림〕` 이 `P_n = F_cr·A_g` 가 됩니다.

  ```
  KCSC_FORMULA_GLOSSARY=/경로/formula_glossary.json
  ```

  `{"<그림 sha256 앞 16자>": "<옮겨 적은 식>"}` 형태입니다. **열쇠가 절 번호가 아니라 그림 자체**라,
  사전에 담아 둔 절이 아니어도 같은 기호·같은 식이면 그대로 듣습니다.

  ★**사전은 이 패키지에 들어 있지 않습니다.** 기준 원문을 동봉·재배포하지 않는다는 방침 때문입니다
  (LICENSE 의 KCSC 고지 참조). 사전을 주지 않으면 지금까지처럼 `〔그림 N〕` 으로만 나옵니다.
  ★사전에 **없는 그림은 지어내지 않습니다.** 그 자리는 `〔그림 N〕` 으로 남고 `kcsc_formula` 로
  원문 그림을 그대로 볼 수 있습니다. 판독이 미심쩍은 항목은 끝에 `⟨?⟩` 가 붙어 나옵니다.

### 고친 것
- **위·아래첨자가 줄을 갈라 먹던 것을 고쳤습니다.** `mm<sup>2</sup>` 가 `mm` / `2` / `)` 세 줄로
  쪼개져 **`mm2)` 처럼 차원이 틀린 것처럼 보였습니다.** 이제 `mm²` 로 붙여 씁니다.
- **식이 제 줄로 떨어져 문장이 토막 나던 것을 고쳤습니다.** 원문이 식을 별도 노드로 두는 탓에
  "공칭압축강도 / P_n / 은 …" 으로 끊겼습니다. 앞줄이 문장으로 끝나지 않았을 때만 잇습니다 —
  번호 붙은 독립 수식(`(4.2-1)`)은 제 줄에 그대로 둡니다.
- `여기서,` 기호 목록의 `: 설명` 줄을 앞 기호에 붙입니다 (`A_g` / `: 총단면적` → `A_g : 총단면적`).

## 0.2.1 — 2026-08-24

### 고친 것
- **KDS 14 3x 계열의 분야 이름을 `건축` → `강구조` 로 바꿨습니다.**
  기준 이름 자체가 「**강구조** 설계 일반사항」·「**강구조** 부재 설계기준」이고, 1.1 적용범위에도
  건축이라는 말이 없습니다("강구조 부재의 해석 및 설계방법"). `건축` 이라고 못 박아 두니
  **교량에서 못 쓰는 것처럼 읽혔고**, 실제로 그렇게 오해한 답이 나왔습니다.
  옛 이름 `domain='건축'` 도 계속 받습니다(`DOMAIN_ALIASES`).
- **조건에 맞는 트리가 없을 때의 안내를 고쳤습니다.** 전에는 "다른 트리가 필요합니다"에서 끝나
  "그러면 못 한다"로 읽혔습니다. 이제 **상위 트리가 그 조건을 다른 트리로 넘기는 경우가 있다**는
  것과 `design_map()` 으로 확인하라는 안내를 함께 냅니다.
  (실제로 KDS 24 14 31 4.1.4.2 가 압축부재를 KDS 14 31 10 4.2 로 넘깁니다.)

## 0.2.0 — 2026-08-23

첫 공개 판.

### 더해진 것
- **결정트리 35개 공개** (`flows/`) — LRFD 18 · 한계상태설계법 9 · 허용응력설계법 8.
  트리끼리 이어져 있고(이음 118개, 끊긴 곳 0), 각 단계에 근거 조항이 붙어 있습니다.
  라이선스는 코드와 달리 **CC BY-SA 4.0** 입니다.
- `flows/README.md` — **KDS 가 값을 정하지 않은 자리**에 어떤 값이 들어 있는지 표로
  정리했습니다. 쓰기 전에 자기 조직 기준으로 바꾸시라는 안내입니다.

### 바뀐 것
- 트리 검증 상태값은 **`설계자확정`** 입니다.
- 확정 판정을 `flows.is_confirmed()` 한 곳으로 모았습니다. 화면에는 파일에 적힌 값이
  그대로 보이고 판정만 정규화하므로, 나중에 상태값을 바꿀 때 표시와 판정이 엇갈리지
  않습니다. `VERIFY_OK_ALIASES` 에 옛 이름을 넣으면 한 판 동안 둘 다 받아 줍니다.

## 0.1.1

- `design_map` 22초 → 0.3초 (결정트리 파싱 캐시)

## 0.1.0

- 첫 판. 도구 14개 — 원문 조회 7 · 설계 보조 7.
