# 보안

## 인증키 취급

이 도구는 **국가건설기준센터(KCSC) OpenAPI 인증키**를 씁니다.
키는 <https://kcsc.re.kr> 에서 **각자 발급**받습니다. 남의 키를 받아 쓰지 마세요.

키는 **환경변수 `KCSC_API_KEY`** 로만 읽습니다. 패키지는 키를 파일에서 찾지 않고,
어디에도 저장하지 않습니다.

- MCP 설정(`claude_desktop_config.json`)의 `"env"` 에 넣거나, OS 환경변수로 두십시오.
- **`.env` 파일에 적지 마세요** — 이 프로젝트는 `.env` 를 읽지 않습니다.
- **이슈·PR 에 키를 붙이지 마세요.** 로그·설정 파일을 첨부할 때 `KCSC_API_KEY` 값이
  섞이지 않았는지 확인해 주십시오. 실수로 올리셨다면 kcsc.re.kr 에서 재발급받으십시오.

## 취약점 신고

보안 문제는 공개 이슈 대신 GitHub 의
[Security Advisory](https://github.com/lhs1152-lgtm/kcsc-design-mcp/security/advisories/new)
로 비공개 신고해 주십시오.

## 이 도구가 하는 일의 범위

이 서버는 **기준 원문을 조회해 보여 주는 것**까지만 합니다. 구조계산을 대행하지 않고,
결정트리는 검토 순서와 근거 조항일 뿐 검증된 설계도서가 아닙니다.
**값·계산·최종판단은 설계자의 몫입니다.**
