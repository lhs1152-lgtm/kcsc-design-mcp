# 기여 안내

한국어로 편하게 쓰셔도 됩니다.

## 먼저 이슈로

코드든 트리든, **이슈로 먼저 이야기해 주십시오.** 특히 결정트리는 설계 판단이 걸려 있어
서로 다른 실무 관행이 부딪힐 수 있습니다. 무엇을 왜 고치려는지 먼저 맞춰 두면 헛수고가 없습니다.

---

## ★ 결정트리를 고칠 때

### 지켜야 할 것

1. **근거는 실재하는 조항이어야 합니다.**
   `design_validate(path=...)` 를 돌리면 적어 둔 조항이 KCSC API 에 실제로 있는지 확인합니다.
   **통과하지 않은 트리는 받지 않습니다.**

2. **없는 것을 있는 것처럼 만들지 마십시오.**
   근거가 없는 자리는 "없다"고 적는 것이 맞습니다. 비슷한 다른 계열 기준을 끌어다
   메우려면 **준용한다는 사실과 그 이유를 트리에 적어야** 합니다.

3. **설계법이 다르면 다른 트리입니다.**
   교량 KDS 24 14 31 과 강구조 KDS 14 31 10 은 하중조합부터 다른 계열입니다.
   (다만 부재 산정식은 교량이 강구조 기준을 준용하기도 합니다 — 24 14 31 4.1.4.2.)
   한 트리 안에서 섞지 마십시오.

4. **`검증: 설계자확정` 은 본인이 확인한 트리에만 붙이십시오.**
   남의 조직이 만든 값을 그대로 옮기면서 확정 표시를 붙이지 마십시오.
   확인 전 상태는 `draft`, 기계 검사만 통과한 상태는 `기계검사통과` 입니다.

5. **조직마다 다를 수 있는 값은 그렇다고 표시하십시오.**
   KDS 가 값을 정하지 않은 자리에 값을 넣을 때는 `조직 규정` 이라고 적고,
   `flows/README.md` 의 표에 한 줄 추가해 주십시오.

6. **계산식과 값을 넣지 마십시오.**
   트리는 "어느 순서로, 어느 조항을 근거로" 까지입니다. 수식은 기준 원문이 이미지이고,
   넣는 순간 이 도구가 구조계산을 대행하는 것이 됩니다.

### 트리 오류를 발견만 하셨어도 좋습니다

고치지 않고 **제보만 하셔도 큰 도움이 됩니다.** 「트리 오류 제보」 서식을 쓰시면
어느 트리·어느 단계·적힌 근거·실제 조항을 빠뜨리지 않고 적을 수 있습니다.

기계 검사는 조항이 실재하는지까지만 봅니다. **흐름이 실무와 맞는지는 사람이 봐야** 하고,
실제로 기계가 전부 통과시킨 오류(설계법 혼동, 약축 강도를 강축 트리에서 뽑던 것,
연결부에 볼트 검토가 통째로 빠진 것)를 사람이 잡아낸 적이 있습니다.

### 새 트리를 만들려면

```
design_template(member="...", shape="...", method="...")   → 뼈대를 받습니다
（채운 뒤）
design_validate(path="...")                                 → 조항 실재 확인
```

파일 이름은 `부재_단면.yaml` 꼴로 합니다(예: `압축부재_원형강관.yaml`).

---

## 코드를 고칠 때

- **안전선을 지우지 마십시오.** `_SAFETY` · `_FORMULA_NOTE` · `_NO_TREE` ·
  분야 기본값 안내 · "조건에 맞는 트리가 없으면 비슷한 것을 내주지 않는다" —
  이 장치들은 교량 설계에서 잘못된 값이 나가는 것을 막으려고 있습니다.
- **인증키를 코드에 넣지 마십시오.** 키는 환경변수 `KCSC_API_KEY` 로만 읽습니다.
- 주석은 **왜 그렇게 했는지**를 적어 주십시오. 무엇을 하는지는 코드가 이미 말합니다.
- 줄바꿈: 결정트리 YAML 은 CRLF 입니다. `.gitattributes` 가 처리하니 그대로 두십시오.

## 확인 방법

```bash
pip install -e .
export KCSC_FLOWS_DIR=$PWD/flows

python -c "from kcsc_mcp.server import design_map;   print(design_map())"    # 트리 35 · 끊긴 곳 0
python -c "from kcsc_mcp.server import design_flows; print(design_flows())"  # 36행
```

조항 실재 확인까지 하려면 `KCSC_API_KEY` 가 필요합니다.
